var radius=prompt("Enter a radius value in cm"); //Takes in a radius value
var area=Math.PI*(radius**2); //Calculates the area
console.log("Area for the circle of entered radius is "+ area + "cm2");//Outputs the area value in console
